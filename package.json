{
  "name": "bitrix-gulp-template",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "gulp build",
    "start": "gulp",
    "tasks": "gulp --tasks",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
  },
  "devDependencies": {
    "gulp": "^4.0.2"
  }
}
